h2 = @channels[params[:id].to_i-1][:roomname]

= form_for @channelmessage, url: channelmessages_path do |f|
  = f.text_field :content, placeholder: "新規メッセージ"
  = f.hidden_field :from_id, value: current_user.id.to_s
  = f.hidden_field :room_id, value: params[:id]
  = f.submit "送信", class: 'square_btn'
